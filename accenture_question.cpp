// minimum characters to be deleted to make s[i]==s[i+1] for every even integer i.
#include<iostream>
#include<string>
#include<vector>
using namespace std;
int main(){
    cout<<"hello world";
    // string s;
    // cin>>s;
    // vector<int>v(s.length()+1,100000);
    // v[0]=0;
    // for(int i=1;i<=s.length();i++){
    //     v[i]=v[i-1]+1;
    //     for(int j=1;j<i;j++){
    //         if(s[i-1]==s[j-1]){
    //             v[i]=min(v[i],v[j-1]+i-j-1);
    //         }
    //     }
    // }
    // cout<<v[s.length()];
}